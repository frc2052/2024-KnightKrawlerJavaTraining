// An enum in Java is similar to a class, but instead of instances being created
// outside the class they are all defined within the class (usually captialized)
public enum Action {
    // Instances of the Direction enum
    MOVE,
    PEEK
}
